//  Package.json: The package.json file contain descriptive and functional metadata about a project, such as a name, version and dependencies
// if we delete node-module and we have a package.json file so we create the node-module again
// how to create a package.json file to run the npm init command
