// The packages we install locally it will run locally and it is a good practice

// how to install the packages globally run the below commands
// npm install -g <- package name ->
// and then link the package
// npm link <- package name ->
